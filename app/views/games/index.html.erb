<div class="top-50 position-relative col-md-10 col-md-offset-1">
  <h3>Hi <%= current_user.username.capitalize %>, here's your Picksheet for this Week:</h3>

<!-- New Table -->

<table class="table table-bordered table-hover table-responsive">
  <thead>
    <tr>
      <th colspan='3"'>Game A</th>
      <th colspan='3"'>Game B</th>
      <th colspan='1"'>Submit Selection</th>
    </tr>
    <tr>
      <th>
        Home Team
      </th>
      <th>
        Away Team
      </th>
      <th>
        Pref Pick Amount
      </th>
      <th>
        Fave
      </th>
      <th>
        Dog
      </th>
      <th>
        Spread
      </th>
      <th>Please Click Submit</th>
    </tr>
  </thead>
  <tbody>
  <% @week.games.each do |game| %>
  <% check_existing_selection(game) %>
    <%= simple_form_for([game, @selection] ) do |s| %>
        <tr>
          <td>
            <%= s.label :pref_pick_team, game.home_team.name  %>
            <%= s.radio_button :pref_pick_team, game.home_team_id  %>
          </td>
          <td>
            <%= s.label :pref_pick_team, game.away_team.name  %>
            <%= s.radio_button :pref_pick_team, game.away_team_id  %>
          </td>
          <td>
            <%= s.select( :pref_pick_int, pref_pick_integers, {prompt: "Select an Amount"}, { class: "pref_pick_int"} )%>
          </td>
          <td>
            <%= s.label :spread_pick_team, game.home_team.name  %>
            <%= s.radio_button :spread_pick_team, game.home_team_id  %>
          </td>
          <td>
            <%= s.label :spread_pick_team, game.away_team.name  %>
            <%= s.radio_button :spread_pick_team, game.away_team_id  %>
          </td>
          <td>
            <%= game.spread.to_s %>
          </td>
          <td>
            <% if Selection.where(user_id: current_user).where(game_id: game.id).present? %>
              <%= s.submit "Update Selection", class: "btn btn-warning" %>
            <% else %>
              <%= s.submit "New Selection", class: "btn btn-primary" %>
            <% end %>
          </td>
          <%= s.hidden_field :game_id,  value: game.id %>
          <%= s.hidden_field :user_id,  value: current_user.id %>
      <% end %>
        </tr>
    
  <% end %>
  </tbody>
</table>
<div>
  <p><b>Game "A"</b> - Pick the teams that will win the games and rank them from 13-1. Each correct pick earns you points equal to the 13-1 ranking number you assigned it. You may not use the same ranking number (between 13 and 1) twice.</p>
  <p><b>Game "B"</b> - Pick the teams that will beat the point spread. Each correct pick earns you 7 points.</p>
  <p></p>
</div>
</div>

<table class="table table-bordered table-striped table-responsive">
  <thead>
    <tr>
      <th>
        Username
      </th>
      <th>
        Total Points
      </th>
    </tr>
  </thead>
  <tfoot>
    <tr>
      <th class="text-align-center" colspan="2"><a href="/stats">See Full Leaderboard</a></th>
    </tr>
  </tfoot>
  <tbody>
    <!-- if @cumulative_points.present? <- this made nothing render, why? -->
    <% @cumulative_points.each do |score| %>
    <tr>
      <td>
        <%= score[1] %>
      </td>
      <td>
        <%= score[0]  %>
      </td>
    <% end %>
    </tr>
  </tbody>
</table>